<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="3dp"
        android:color="@color/primary" /> <!-- Ganti dengan warna border yang diinginkan -->
    <padding
        android:top="2dp"
        android:left="2dp"
        android:right="2dp"
        android:bottom="2dp" />
    <solid android:color="@android:color/transparent" /> <!-- Warna background yang kosong -->
</shape>
